<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="oval"
    android:useLevel="false">
        <!-- 实心 -->
        <solid android:color="#ff9d77"/>
        <!-- 渐变 -->
        <gradient
            android:type="radial"
            android:gradientRadius="500"
            android:startColor="@color/blue_title"
            android:endColor="@color/blue_press"
            android:angle="360"/>
        <!-- 描边 -->
        <stroke
            android:width="10dp"
            android:color="@color/skyblue"/>
        <!-- 圆角 -->
        <corners
            android:radius="2dp"/>
        <padding
            android:left="10dp"
            android:top="10dp"
            android:right="10dp"
            android:bottom="10dp"/>
</shape>
